// Nested source-root entry (web/src/main.js). Rooted as a production entry by
// the nested-src convention. Imports a feature module directly.
import { runFeature } from "./feature.js";

runFeature();
